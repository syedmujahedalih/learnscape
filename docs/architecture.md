# Architecture

One React shell owns source interpretation, blueprints, missions, and library navigation. Independent deterministic engines implement titration, circuits, and statistics. `app/api/analyze` routes pasted-text classification to local llama.cpp or optional GPT‑5.6 and validates the resulting blueprint.
