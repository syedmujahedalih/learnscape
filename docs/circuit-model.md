# Circuit model

The circuit is one ideal closed DC loop. For a closed switch, `I = V / R` and `P = VI`; an open switch has zero current. Brightness is a qualitative visual mapping of calculated current.
