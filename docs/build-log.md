# Build log

- Codex initialized the Sites-compatible React project and built the interactive prototype.
- A shared Zod blueprint contract and deterministic subject engines keep scientific calculations separate from interface rendering.
- Local llama.cpp is the default analysis provider; GPT‑5.6 is optional and server-side.
